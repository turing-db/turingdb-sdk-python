[build-system]
requires = ["scikit-build-core>=0.8.0"]
build-backend = "scikit_build_core.build"

[project]
name = "turingdb"
version = "0.1.0"
description = "TuringDB Python extension"
dependencies = ["pandas", "numpy>2"]
requires-python = ">=3.8"
